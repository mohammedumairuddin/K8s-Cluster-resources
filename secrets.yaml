apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: restorent
type: Opaque
stringData:
  MONGODB_URI: "REPLACE_WITH_YOUR_MONGODB_URI"  # e.g. mongodb+srv://user:pass@cluster/dbname
  SESSION_SECRET: "change-me"
